#details
  #c1
    %p #{label_tag('Description')}: #{@project.description}
    %p #{label_tag('Created on')}: #{@project.created_on.strftime('%b %d %Y')}

  #c2
    %p #{label_tag('Completed')}: #{throughput(@project)}
    %p #{label_tag('Eff.')} (avg.): #{efficiency(@months)}
  
  #c3
    %p #{label_tag('Velocity')} (avg.): #{velocity(@months)} 
    %p #{label_tag('1 point')} (avg.): #{point_duration(@project)} 

  #sep
