- @title = "Project list"
%h2 Project list

- if @projects.count == 0
  = You don't seem to have any projects configured, please configure some.

- @projects.each do |project|
  %h3= link_to project.name, project
