- title "User Project configurations"

%h2 Registered project configurations

%div#configurations
  = render("configurations", :collection => @configurations)

%hr
%h3 Search for project configurations
- form_tag user_project_config_index_path(current_user), :remote => true do
  %p
    = label_tag :api_key
    = text_field_tag :api_key, nil, :size => 100
  %p= submit_tag "Search"

%div#search_results
